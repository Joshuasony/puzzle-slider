{{#app-sidenav currentRouteName=currentRouteName}}
  {{~app-toolbar
    currentRouteName = currentRouteName
    onback = (route-action "back")
    ~}}
  <main class="flex layout-column" role="main">
    {{~liquid-outlet class="application-outlet flex layout-column"~}}
  </main>
{{/app-sidenav}}
{{ember-notify messageStyle="bootstrap"}}
<div class="app-version">{{app-version}}</div>
